# kubehello – Tiny (30KB) Docker image for demoing Kubernetes :wave:

## Example usage

```bash
$ kubectl create -f pod.yaml
pod/kubehello created
$ kubectl logs kubehello
Hello from demo-namespace!
```
